'use babel';

import IridiumSnippetsView from '../lib/iridium-snippets-view';

describe('IridiumSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
